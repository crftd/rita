import { describe, expect, it } from '@jest/globals';

import Rita from './Rita';

describe('Rita', () => {
  describe('parseArguments', () => {
    it('should run', () => {
      // Arrange
      // Act
      Rita.parseArguments();
      // Assert
    });
  });
});
